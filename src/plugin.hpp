#pragma once
#include <rack.hpp>

using namespace rack;

// Forward declare the plugin instance 
extern plugin::Plugin* pluginInstance;

// Forward declare the module model
extern plugin::Model* modelCurveAndDrag;
